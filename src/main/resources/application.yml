spring:
  profiles:
    include: private

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/delivery
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
